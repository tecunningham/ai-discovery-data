# Exact versions are part of the containerized figure ABI used locally and in
# CI (tools/figures.Dockerfile): bumping any of them can change PNG bytes, so
# a bump comes with a figure re-render, never alone.
#
# Direct dependencies:
Markdown==3.10
matplotlib==3.11.1
PyYAML==6.0.3
# matplotlib's dependency closure, pinned for the same ABI — unused directly,
# so do not prune them on an unused-import sweep:
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
numpy==2.5.2
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
